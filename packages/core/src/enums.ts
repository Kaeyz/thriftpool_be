export enum AppEnv {
  DEV = "dev",
  TEST = "test",
  PROD = "prod",
}

export enum Platform {
  Community = "community",
  Admin = "admin",
}

export enum CurrencyCode {
  NGN = "NGN",
  USD = "USD",
  EUR = "EUR",
  GBP = "GBP",
}
